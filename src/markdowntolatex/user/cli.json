{
    "version": {
        "flags": [
            "-v",
            "--version"
        ],
        "params": {
            "dest": "version",
            "action": "store_true",
            "default": false,
            "help": "Package version, following the usual \"major.minor.micro\" scheme."
        }
    }
}